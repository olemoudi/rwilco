// Classes the Compose compiler cannot see into and would otherwise call unstable, which made
// every card on Home non-skippable and recomposed in full on each minute tick (0.68.0).
// java.time values are immutable; the model is a Kotlin module of data classes over them.
java.time.*
dev.rwilco.model.*
