<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Debug only. Seed or clear demo data from adb: am broadcast -n dev.rwilco/.debug.DemoSeedReceiver with a string extra "seed" of "demo" or "clear". -->
        <receiver
            android:name=".debug.DemoSeedReceiver"
            android:exported="true" />
    </application>
</manifest>
